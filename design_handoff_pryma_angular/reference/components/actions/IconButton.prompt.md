Circular icon-only action; always pass \`label\` for the accessible name.

\`\`\`jsx
<IconButton label="Open menu" variant="glass"><Icon name="menu" /></IconButton>
\`\`\`

\`ghost\` for dense toolbars, \`glass\` over gradients and nav bars, \`neon\` for a single emphasised action.
