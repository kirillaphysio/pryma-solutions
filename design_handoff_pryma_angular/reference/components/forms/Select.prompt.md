Dropdown built on the native \`<select>\` so it stays keyboard- and mobile-correct.

\`\`\`jsx
<Select label="Team size" placeholder="Choose one" options={['1–10', '11–50', '51–200', '200+']} />
\`\`\`

Same well and ring as TextInput. The chevron is Lucide \`chevron-down\`, masked to \`--text-mute\`.
