Brand lockup wherever a logo would go — set in Space Grotesk caps, not an image.

\`\`\`jsx
<Wordmark size={22} />
<Wordmark size={18} tone="mono" suffix={null} />
\`\`\`

No logo file was supplied with the source material, so the wordmark is typographic by design. Replace \`assets/logo.svg\` and this component together if a real mark arrives.
