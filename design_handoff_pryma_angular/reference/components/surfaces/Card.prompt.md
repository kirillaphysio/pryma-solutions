The system's container surface — glass over the mesh gradient by default.

\`\`\`jsx
<Card variant="surface" interactive glow="cyan">…</Card>
<Card variant="band">Closing CTA copy</Card>
\`\`\`

Cards use a 1px hairline via inset shadow, never a CSS border, and 16px radius. \`band\` is the full-width closing CTA surface at 48px padding. Never put a MockupFrame inside a Card.
