One pricing tier, composed from Card + Button + Eyebrow + Icon.

\`\`\`jsx
<PricingCard tier="Studio" price="$79" blurb="For product teams shipping weekly."
  features={['Unlimited projects', 'Design tokens sync', 'Priority review']}
  featured badge="Most popular" cta="Start 14-day trial" />
\`\`\`

Mark exactly one tier \`featured\` — it gets the gradient fill and the pink glow, and its CTA switches to the \`onAccent\` outline button. Ticks are Lucide \`check\` in cyan.
